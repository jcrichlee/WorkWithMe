{
  "name": "workwithme-backend",
  "version": "1.0.0",
  "type": "module",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js",
    "lint": "eslint ./src --ext .js",
    "format": "prettier --write ./src",
    "heroku-postbuild": "echo 'No build step required for backend.'"
  },
  "dependencies": {
    "cookie-parser": "^1.x",
    "cors": "^2.x",
    "dotenv": "^16.x",
    "express": "^4.x",
    "jsonwebtoken": "^9.0.2",
    "mongoose": "^7.x",
    "morgan": "^1.x"
  },
  "devDependencies": {
    "eslint": "^8.x",
    "nodemon": "^3.x",
    "prettier": "^3.x"
  }
}
